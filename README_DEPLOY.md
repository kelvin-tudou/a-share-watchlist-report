# V6.6B Deploy README

当前状态：尚未部署到公网。

本目录是静态站点包，打开 `index.html` 可查看 V6.6B 主报告。部署前只上传本目录内的公开静态文件，不上传 token、SQLite、缓存原始数据、源码私密配置或 `.git`。

推荐最简单路径：先使用 Cloudflare Pages 的 Direct Upload 或创建一个只放静态报告的公开 GitHub Pages 仓库。
